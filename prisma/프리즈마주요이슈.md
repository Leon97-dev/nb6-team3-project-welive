ApprovalStatus에 NEED_UPDATE가 없습니다. 인증 명세서에는 가입 상태가 PENDING, APPROVED, REJECTED, NEED_UPDATE로 잡혀 있는데 현재 스키마는 3개만 있습니다. 이 상태값이 없으면 상태 변경 API나 응답 매핑에서 명세를 그대로 못 맞춥니다. schema.prisma (line 21)

공지사항 명세에는 boardId가 필수인데, Notice 모델에는 boardId 자체가 없습니다. 현재 Board는 complaints, polls만 연결되어 있고 notices 연결도 없습니다. 그래서 /api/notices 생성/수정 명세를 그대로 구현할 수 없습니다. schema.prisma (line 217) schema.prisma (line 236)

이벤트/일정 모델이 Poll 일정을 직접 표현하지 못합니다. ApartmentSchedule은 noticeId만 가지고 있는데, 명세서 8번의 event API는 NOTICE와 POLL 이벤트를 모두 다룹니다. 지금 구조로는 poll 이벤트를 DB에 정식 연결할 수 없습니다. schema.prisma (line 388)

회원가입 중복 검사 기준과 User 제약이 안 맞습니다. auth 명세에는 아이디, 이메일, 전화번호 중복 시 409가 나와야 하는데, User.contact에는 @unique가 없습니다. 서비스 코드에서 직접 막을 수는 있지만 DB가 안 받쳐주면 중복 데이터가 들어갈 수 있습니다. schema.prisma (line 109) schema.prisma (line 112) schema.prisma (line 113)

중요하지만 급한 정도는 한 단계 낮은 이슈

알림 스키마와 명세가 다릅니다. 명세서 8은 complaintId, noticeId, pollId, 다양한 notificationType을 기대하는데, 현재는 relatedBoardType, relatedBoardId만 있고 enum도 더 좁습니다. API에서 억지로 매핑은 가능하지만 나중에 타입이 꼬일 가능성이 큽니다. schema.prisma (line 91) schema.prisma (line 411)

User.isRegistered는 값이 중복 저장되는 구조라 위험합니다. 이미 residentRoster 연결이나 가입된 User 존재로도 어느 정도 판단 가능한데, 별도 boolean까지 두면 값이 틀어질 수 있습니다. 특히 입주민 명부와 실제 계정 연결이 바뀔 때 sync 실수가 나기 쉽습니다. schema.prisma (line 116) schema.prisma (line 132)

ID가 전부 cuid()인데 명세 예시는 UUID 계열로 적혀 있습니다. 백엔드만 보면 큰 문제는 아니지만, 프론트나 Swagger 문서에서 UUID 전제로 검증하면 충돌할 수 있습니다. schema.prisma (line 108)

Comment는 다형 관계라서 실무적으로 흔한 방식이긴 한데, FK가 없어서 잘못된 boardId를 넣어도 DB 차원에서 막지 못합니다. 지금 단계에서는 단순해서 괜찮지만, 서비스 레이어 검증을 빼먹으면 데이터 무결성이 약합니다. schema.prisma (line 367)
